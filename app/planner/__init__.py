"""Purpose: marks `app.planner` as a Python package — decomposes a task
goal into a validated, dependency-ordered Plan. Empty by design — import
directly from `app.planner.planner`, not from this file.
"""
