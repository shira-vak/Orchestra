"""Purpose: constants used only within app/planner/ — see CLAUDE.md's No Magic Values table."""

PLANNER_MAX_TOKENS = 2048

# total attempts (including the first) before raising InvalidPlanError
PLANNER_MAX_ATTEMPTS = 3
