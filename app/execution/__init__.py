"""Purpose: marks `app.execution` as a Python package — runs a validated
Plan's steps respecting dependencies, in parallel where the plan allows.
Empty by design — import directly from `app.execution.engine`, not from
this file.
"""
