"""Purpose: marks `app.synthesis` as a Python package — combines a plan's step
outputs into one final result. Empty by design — import directly from
`app.synthesis.synthesizer`, not from this file.
"""
