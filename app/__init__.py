"""Purpose: marks `app` as a Python package. Deliberately empty otherwise —
no re-exports here (see CLAUDE.md's "no __init__.py barrels" rule); import
from the real module path (e.g. `app.config`), not from this file.
"""
