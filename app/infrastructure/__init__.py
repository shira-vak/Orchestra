"""Purpose: marks `app.infrastructure` as a Python package — the umbrella for
everything that talks to an external system (Postgres, the Anthropic API).
Empty by design — import from `app.infrastructure.db` / `app.infrastructure.llm`.
"""
